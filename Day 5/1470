class Solution {
    public int[] shuffle(int[] nums, int n) {
        int j=0;
        int [] res=new int[nums.length];
        for(int i=0;i<n;i++){
            res[j++]=nums[i];
            res[j++]=nums[n+i];
        }
        return res;
    }
}
